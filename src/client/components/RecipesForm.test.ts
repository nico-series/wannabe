import { describe, expect, test } from 'vitest';

// brainstorming relevant tests
// a lot of these are going to require refinement
// any validation should be verified
describe('goal input', () => {
    test('should populate a list', () => {

    })

    test('should complain about entry of unlisted values', () => {

    })
})

describe('quantity input', () => {
    // wym
    test('should complain about invalid quantities', () => {

    })
})

describe('form', () => {
    test('should submit with valid data', () => {

    })
})